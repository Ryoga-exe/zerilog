const Clock = clock_posedge;
const Reset = reset_async_low;

pub module Counter(
    clk: input Clock,
    rst: input Reset,
) {
    var r: logic8;

    always_ff {
        if_reset {
            r = 0;
        } else {
            r += 1;
        }
    }
}
